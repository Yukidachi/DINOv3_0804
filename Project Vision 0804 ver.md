# Project : Vision 0804 ver.

# 主要目标 ：进一步探究 DINO V3 教师与 Mobilenet V2 / V3 为学生的知识蒸馏

# 0 . 研究内容

**i. PCA 为何优于 1x1 Conv    A0-A6 实验**

**ii. 响应蒸馏 ( Response-Based Knowledge ) 是否有必要    K0-K4 实验**

**iii. 引入关系蒸馏 ( Relation-Based Knowledge)    R0-R5 实验**

**iv. 分布知识蒸馏 ( 特征层 MSE 基础上新增约束 )    D0-D2 实验**

**v. 激活函数    H0-H3 实验**

# 00. 准备工作

## 00a. **锁定Test集**

先固定如下协议：

| **本地划分** | **来源** | **城市** | **图像数** | **用途** |
| --- | --- | --- | --- | --- |
| `train_local` | 官方 train | 除下列 4 城市外的 14 城市 | 2530 | 训练模型、拟合 Scaler/PCA |
| `dev_local` | 官方 train | darmstadt、jena、krefeld、weimar | 445 | 早停、超参数和模型选择 |
| `test_local` | 官方 val | frankfurt、lindau、munster | 500 | 协议冻结后的本地最终评估 |
| `official_test` | 官方 test | 6 个 test 城市 | 1525 | 可选的一次性官网提交 |

这相当于“从 train 拆开发集，并把官方 val 改作本地 test”，比“从 train 随机拆 test”更合理，原因是：

1. 官方 val 有真实精细标注，能够直接本地评分；
2. train、val 原本按城市隔离，`test_local` 的域独立性更强；
3. `dev_local` 也按完整城市拆分，避免相邻场景泄漏；
4. 官方 test 保持未见，只在确需官方结果时提交。

必须生成并版本锁定 `train_local.txt`、`dev_local.txt`、`test_local.txt`，保存 SHA-256、划分日期和规则。PCA、归一化统计、困难样本筛选和类别权重只能使用 `train_local`。

若今后需要官方 test 成绩，可以在本地方案完全冻结后，用同一配置在官方 train+val 上重训一个“官网提交模型”。该模型及官网成绩必须与上述 2530/445/500 本地协议结果分开报告。

## 00b. 固定协议

| **项目** | **固定要求** |
| --- | --- |
| 数据 | `train_local=2530`：官方 train 排除 darmstadt/jena/krefeld/weimar；`dev_local=445`：上述 4 城市；`test_local=500`：官方 val；官方 test 1525 张仅作网站提交 |
| 标签 | 官方 `labelIds -> trainIds 0..18`，其余为 `ignore_index=255`；不用 color mask，不用 test 占位 mask 评分 |
| 输入 | train：scale `[0.5,2.0]`、crop `512×1024`、水平翻转；dev/test：`1024×2048` 单尺度、无翻转 |
| 模型 | 教师 DINOv3 ConvNeXt-T+R-ASPP；学生 V2+R-ASPP；学生 V3-Small+LR-ASPP |
| 特征 | 全模型 `output_stride=16`；固定对齐 OS=4/8/16；teacher/student 共用同一几何增强 |
| 监督 | 19 类 pixel CE；主表不加入 Dice、Focal、辅助头、多尺度或 flip inference |
| KD 默认 | `T=4`、`lambda_logit=0.5`、`lambda_feat=1.0`；辅助项前 5% step 线性 warm-up |
| 预算 | scratch 监督训练 80k steps；A 组无标签预训练 40k+冻结 head probe 40k；global batch 建议 8，显存不足用梯度累积 |
| 优化 | 学生首选 SGD+poly LR；教师首选 AdamW 分组 LR；只允许在 P0 预跑调整一次，锁定后不得因实验而变 |
| 随机性 | 筛选 seed `42`；确认 seed `42/3407/260805`；同组固定 batch 顺序和初始化 |
| 指标 | 19 类 mIoU 主指标；另报每类 IoU、mAcc、pixel Acc、boundary F1 |
| 选择 | 只按 dev mIoU 选 checkpoint；test_local 在方案冻结前不可查看；训练 adapter 在效率测试前移除 |

## 00c. 基线

| 基线 | T0 | DINOv3+R-ASPP | 冻结 backbone，只训练 head | 3 seed；冻结 DINO 分割基线 |
| --- | --- | --- | --- | --- |
| 基线 | T1 | DINOv3+R-ASPP | head warm-up 后只解冻最后 stage | 3 seed；从 T0/T1 仅按 dev 锁定一个教师及哈希 |
| 基线 | S2-0 | V2+R-ASPP | `weights=None`，端到端 CE，无 KD | 3 seed；V2 主基线和效率指标 |
| 基线 | S2-P | V2+R-ASPP | 唯一变化为 ImageNette2-320 预训练初始化 | 3 seed；与 scratch 分表报告；标记为资源受限预训练基线 |

运行结果：

| **实验** | **初始化/训练方式** | **seed 数** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T0 | DINOv3 backbone 冻结，只训 R-ASPP head | 3 | `0.681911 ± 0.001092` | `0.790034 ± 0.001896` | `0.929923 ± 0.004194` | `0.696752 ± 0.003737` | `0.341351 ± 0.002468` |
| T1 | head warm-up 后解冻 DINOv3 最后 stage | 3 | `0.776024 ± 0.002086` | `0.864582 ± 0.002784` | `0.959541 ± 0.000310` | `0.790174 ± 0.003894` | `0.574451 ± 0.000506` |
| S2-F | 随机 MobileNetV2 冻结，只训 R-ASPP head | 1 | `0.019125` | `0.052632` | `0.363378` | `0.000000` | `0.000000` |
| S2-0 | MobileNetV2 scratch，端到端 CE | 3 | `0.495653 ± 0.005220` | `0.576490 ± 0.008605` | `0.886465 ± 0.002921` | `0.391439 ± 0.008099` | `0.382988 ± 0.003734` |
| S2-P | ImageNette2-320 预训练，端到端 CE | 3 | `0.494730 ± 0.008502` | `0.569867 ± 0.012074` | `0.891672 ± 0.005036` | `0.386953 ± 0.013837` | `0.391918 ± 0.006024` |

# 1. A组实验 —— PCA实验

## 1.1 实验目标

1. PCA 本身是否优于任意低秩投影；
2. `StandardScaler` 是否必要；
3. 固定投影是否比可学习投影稳定；
4. 把目标压到学生通道数是否比强迫学生拟合完整教师空间容易；
5. 学生侧是否需要一个可学习坐标变换。

## 1.2 实验方法

### **a. PCA的执行流程**

- **数据准备与确定性采样**：
    - 数据源：固定使用 `datasets/cityscapes/train_local.txt` 中的 **2530 张图**（清单哈希已锁定）。
    - 输入视图：将原始图像缩放到 `512×1024`，使用 ImageNet 均值/std 归一化，**不裁剪、不翻转**。
    - 分层采样：针对 `OS=4/8/16` 三层分别进行。每层抽取 **200,000** 个 token（特征点）。分配规则是每张图先给 79 个，再通过哈希排序给其中 330 张图各加 1 个（即 79~80 个/图）。
    - 空间位置：为避免集中，将特征图划分为 `8×8` 格子，通过 **token slot 和固定哈希**（`seed + 路径 + 层名`）在格子内确定具体的 `(row, col)`。
- **第一遍：拟合 StandardScaler** ：
    - 按 manifest 顺序读取上述选定的 200k token。
    - 使用 `partial_fit`（或 Welford 在线算法）计算每个通道的均值 (`mean_`) 和方差，得到 `scale_`（标准差，方差为0时置1）。
- **第二遍：拟合 PCA** ：
    - **重新**按完全相同的 manifest 读取 token。
    - 用第一遍得到的 `StandardScaler` 对 token 进行标准化（减去均值，除以 scale）。
    - 使用 **`IncrementalPCA`**（batch_size=8192）逐块拟合，`n_components` 分别等于学生通道数（24/32/320），且 **`whiten=False`**（不进行白化）

### b. PCA 转 1x1 Conv 投影层

用 `Conv2d(C_t, d_l, kernel_size=1, bias=True)` 逐位置执行上述变换。卷积权重和偏置为：

$$
W_{o,i}=\frac{V_{o,i}}{\sigma_{s,i}}
$$

$$
b_o=-\sum_i V_{o,i}\left(\frac{\mu_{s,i}}{\sigma_{s,i}}+\mu_{p,i}\right)
$$

- 数学具体实现
    
    为了将 sklearn 的 PCA 变换精确移植到 PyTorch 的 `1×1` 卷积层，核心数学操作是**将“标准化 → PCA旋转/降维”的两步仿射变换，合并为一个带偏置的线性层（`Wx + b`）**。
    
    以下是详细的数学推导和操作拆解（完全对应你方案中的第 4.1 和 4.2 节）：
    
    ### 1. 定义输入与参数（符号说明）
    
    对于某一层（如 OS=16），设教师特征图在某个空间位置的向量为 $(x \in \mathbb{R}^{C_t})$（$C_t$ 为教师通道数，如 768）。
    
    - **StandardScaler 参数**（拟合于原始特征）：
        - $\mu_s$：`scaler.mean_`，形状 [$C_t$]
        - $\sigma_s$：`scaler.scale_`，形状 [$C_t$]
    - **PCA 参数**（拟合于标准化后的特征）：
        - $\mu_p$：`pca.mean_`，形状 [$C_t$]（**注意**：这是标准化后特征的均值，而非原始均值）
        - $V$：`pca.components_`，形状 [$d_l, C_t$]（$d_l$ 为学生通道数，如 320；每一行是一个主成分）
    
    ---
    
    ### 2. sklearn 中的原始变换公式
    
    根据方案第 4.1 节，sklearn 的 `StandardScaler` 配合 `PCA.transform` 对向量 $x$ 的操作是：
    
    $y = \left( \frac{x - \mu_s}{\sigma_s} - \mu_p \right) V^T$
    
    其中除法为逐元素相除（$\oslash$）。展开括号：
    $y = \left( \frac{x}{\sigma_s} \right) V^T - \left( \frac{\mu_s}{\sigma_s} \right) V^T - \mu_p V^T$
    
    ---
    
    ### 3. 转化为卷积的权重 $W$ 和偏置 $b$
    
    `1×1` 卷积对输入 $x$ 的操作为仿射变换：$y = W x + b$。我们需要把上面的公式改写为这种形式。
    
    #### 推导权重 $W$（形状：$[d_l, C_t]$ ）
    
    将逐元素除法变为矩阵乘法（引入对角矩阵 $\text{diag}(1/\sigma_s)$），变换式中第一项可以写作：
    
    $\left( \frac{x}{\sigma_s} \right) V^T = x^T \cdot \text{diag}(1/\sigma_s) \cdot V^T$
    
    （若视 $x$ 为列向量，结果为 $1 \times d_l$，转置后得到 $d_l \times 1$ 列向量）。提取(  $x$ 的系数，得到卷积权重：
    
    $W = V \cdot \text{diag}(1/\sigma_s)$
    
    即矩阵 $V$ 的每一列（对应输入通道 $i$）除以该通道的标准差 $\sigma_{s,i}$。写成索引形式（方案中的公式）：
    
    $W_{o,i} = \frac{V_{o,i}}{\sigma_{s,i}}$
    
    > **物理含义**：卷积的权重不仅包含了 PCA 的主成分方向（$V$），还**预先嵌入了 StandardScaler 的尺度归一化**，这样输入 $x$ 进入卷积后就不需要额外做除法了。
    > 
    
    #### 推导偏置 $b$（形状：$[d_l]$ ）
    
    将原公式的后两项合并为偏置项（不含  $x$ 的部分）：
    
    $b = - \left( \frac{\mu_s}{\sigma_s} \right) V^T - \mu_p V^T$
    
    提取公因子 $V^T$（注意维度对齐，写成矩阵乘法形式）：
    
    $b = - \left( \frac{\mu_s}{\sigma_s} + \mu_p \right) V^T$
    
    写成标量索引形式（方案中的公式）：
    
    $b_o = -\sum_{i} V_{o,i} \left( \frac{\mu_{s,i}}{\sigma_{s,i}} + \mu_{p,i} \right)$
    
    > **关键易错点**：这里的 $\mu_p$ 是 PCA 在**标准化空间**中的中心，而 $\mu_s$ 是原始特征空间的均值。方案第 4.2 节特别强调：**不能误用原始特征均值 `scaler.mean_` 替代 $\mu_p$**，也不能漏掉 $\mu_p$。
    > 
    
    ---
    
    ### 4. 为什么要如此精确地“合并”？
    
    - **推理效率**：训练时（A0/A1/A3/A4）不需要保留 Sklearn 的 Scaler 和 PCA 两个对象，只需一个固定的 `1×1` 卷积层，计算速度更快且完全在 GPU 上。
    - **可微调性（A2）**：合并后，`W` 和 `b` 作为标准的卷积参数，可以被优化器更新，允许我们在 PCA 优秀初始值的基础上进行微小的适配。
    - **等价性验收**：方案要求 **`max_abs_error ≤ 1e-5`**，这确保了我们用 PyTorch 重写的“卷积投影”与离线 sklearn 计算的“PCA 投影”在数学上是**完全等价的**，从而保证 A1 和 A0 的差异只来源于实现形式，而非计算误差。

## 1.3 实验内容

| **阶段** | **编号** | **模型/目标** | **本次实验的唯一要求** | **完成条件与产物** |
| --- | --- | --- | --- | --- |
| 基线 | S2-F | 随机 V2+R-ASPP | `weights=None`，冻结 backbone，只训练 head | 1 seed；A 组 probe 下界 |
| 基线 | S2-0 | V2+R-ASPP | `weights=None`，端到端 CE，无 KD | 3 seed；V2 主基线和效率指标 |
| 基线 | S2-P | V2+R-ASPP | 唯一变化为 ImageNette2-320 预训练初始化 | 3 seed；与 scratch 分表报告；标记为资源受限预训练基线 |
| 投影 | A0 | 固定 PCA，T→S | 每层 StandardScaler+PCA 到学生原生通道；无标签预训练 | seed 42；probe mIoU、稀有类 IoU、CKA |
| 投影 | A0-FT | A0 端到端微调 | 从 A0 最优 probe checkpoint（backbone+head）出发，解冻整个 backbone，按 S2-0 的 80k step、SGD+poly、端到端 pixel CE 微调；不加任何 KD 项 | seed 42；fine-tune mIoU；与 S2-0 scratch 基线和 A0 probe 对比；记录源 probe checkpoint 与初始/最终 backbone 哈希 |
| 投影 | A1 | 固定 1×1 Conv，T→S | 将 A0 的 Scaler+PCA 精确写入 Conv | 训练前各层 `max_abs_error<=1e-5`；指标应等价 A0 |
| 投影 | A1-FT | A1 端到端微调 | 从 A1 最优 probe checkpoint 出发，其余与 A0-FT 完全相同 | seed 42；fine-tune mIoU；数值上应与 A0-FT 接近（A1 骨干等价于 A0） |
| 投影 | A2 | 可学习 PCA-Conv，T→S | A1 初始化，adapter LR=学生 LR×0.1 | seed 42；判断固定锚点是否关键 |
| 投影 | A3 | 固定随机正交，T→S | 保留 StandardScaler，仅把 PCA 换为固定随机正交投影 | seed 42；保存正交误差；判断主方向作用 |
| 投影 | A4 | 无 Scaler 的 PCA，T→S | 相对 A0 只取消 StandardScaler | seed 42；判断尺度校准作用 |
| 投影 | A5 | 共享瓶颈 | 教师固定 PCA→`d_l=C_s`；学生可学习 1×1 Conv→同维度 | seed 42；推理移除 adapter；判断学生坐标自由度 |
| 投影 | A5-FT | A5 端到端微调 | 从 A5 最优 probe checkpoint（adapter 已在 probe 前移除）出发，其余与 A0-FT 完全相同 | seed 42；fine-tune mIoU；当前单次 probe 最优候选的端到端结果 |
| 投影 | A6 | 完整教师空间，S→T | 无 PCA；学生 1×1 Conv 从 `C_s` 扩到 `C_t` | seed 42；判断完整教师空间是否过难 |

## 1.4 实验结果

### a1. 性能指标 ( seed = 42 )

| **实验** | **教师侧变换** | **学生侧** | **probe mIoU
          ↑** | **mAcc
              ↑** | **pixel Acc
              ↑** | **small-object mIoU
               ↑** | **boundary F1
             ↑** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A0 | StandardScaler+PCA，T→S | 无 | `0.38864` | `0.46473` | `0.88722` | `0.23636` | `0.33737` |
| A1 | 固定 1×1 Conv（PCA 融合） | 无 | `0.39447` | `0.47117` | `0.88997` | `0.24211` | `0.34030` |
| A2 | PCA-Conv 可训练 | 教师侧目标可动 | `0.23590` | `0.30702` | `0.80315` | `0.10798` | `0.16671` |
| A3 | StandardScaler+随机正交 | 无 | `0.31077` | `0.38277` | `0.85508` | `0.15683` | `0.26350` |
| A4 | PCA，无 StandardScaler | 无 | `0.37807` | `0.46960` | `0.86232` | `0.21850` | `0.31961` |
| A5 | StandardScaler+PCA | 学生 C_s→C_s Conv | **0.39888** | **0.47966** | **0.89560** | **0.24685** | **0.34329** |
| A6 | StandardScaler，完整教师空间 | 学生 C_s→C_t Conv | `0.27407` | `0.33678` | `0.84304` | `0.13136` | `0.22089` |
| **S2-F** | 随机冻结骨干 | 只训 head | `0.01913` | `0.05263` | `0.36338` | `0.00000` | `0.00000` |
| **S2-0** | scratch 端到端 CE | 全模型训练 | `0.49565 ± 0.00522` | `0.57649 ± 0.00861` | `0.88647 ± 0.00292` | `0.39144 ± 0.00810` | `0.38299 ± 0.00373` |

首轮结果排序为 A5 > A1 > A0 > A4 > A3 > A6 > A2。A1≈A0 说明显式 PCA 与固定 1×1 Conv 在训练效果上等价；A3/A4/A6 的相对退步分别说明 PCA 主方向、StandardScaler 和低维目标空间均有作用；A2 的明显退步说明教师侧可学习目标破坏了固定锚定。

**关于A组训练预算：**

特征预训练预算：40,000 optimizer steps；只优化学生 backbone，以及 A2/A5/A6 中明确存在的训练期 adapter。
probe 预算：冻结学生 backbone 后训练同一个 19 类 R-ASPP head 40,000 optimizer steps。probe 的初始化、global batch、学习率和数据顺序对所有 A 组一致。

而 A0-A6 的指标低于 S2-0 端到端训练基线便是由于 A0-A6 分两阶段训练，之后补充了 A0/A1/A5 的 80000 steps 的端到到微调预算实验（A0-FT/A1-FT/A5-FT），指标均大幅超过 S2-0 基线，表面A组使用 PCA / 1x1 Conv 辅助蒸馏手段有明显收益。

**3 seed 扩充：**

| **实验** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| A0 | `0.39268 ± 0.00664` | `0.46763 ± 0.00863` | `0.89207 ± 0.00433` | `0.23987 ± 0.00699` | `0.33987 ± 0.00387` |
| A5 | **0.39888 ± 0.00117** | **0.47726 ± 0.00312** | **0.89709 ± 0.00231** | **0.24530 ± 0.00167** | **0.34535 ± 0.00265** |
| A6 | `0.27400 ± 0.00157` | `0.33836 ± 0.00147` | `0.84240 ± 0.00469` | `0.13342 ± 0.00563` | `0.22251 ± 0.00266` |

### a2. CKA （中心核对齐）和 对齐残差 ( seed = 42 )

| **实验** | **CKA os4** | **CKA os8** | **CKA os16** | **残差 os4** | **残差 os8** | **残差 os16** |
| --- | --- | --- | --- | --- | --- | --- |
| A0 | 0.761 | 0.623 | 0.824 | 0.370 | 0.552 | 0.444 |
| A1 | 0.771 | 0.628 | 0.833 | 0.358 | 0.546 | 0.435 |
| A2 | 0.767 | 0.607 | 0.502 | 0.385 | 0.666 | 0.972 |
| A3 | 0.532 | 0.410 | 0.764 | 0.620 | 0.772 | 0.558 |
| A4 | 0.408 | 0.381 | 0.791 | 1.065 | 1.078 | 0.486 |
| A5 | 0.757 | 0.618 | 0.506 | 0.399 | 0.567 | 0.766 |
| A6 | — | — | — | — | — | — |

残差定义为 `‖s-p(t)‖²/‖p(t)‖²`。A6 在 adapter 移除后学生和教师通道不同，不计算逐元素残差。

1. A1 和 A0 的 CKA 和残差几乎一致，表面将显式 PCA 融合为固定 1x1 卷积在数值上和训练动态上均为引入误差
2. A5 的 probe mIoU（0.3988）高于 A0（0.3886），但其 **OS=16（高层）的 CKA 仅为 0.506**，远低于 A0 的 0.824；同时 **OS=16 的残差高达 0.766**，远高于 A0 的 0.444。A5 的学生侧可学习坐标变换，**主动破坏了学生高层特征（OS=16）与教师高层特征的结构相似性（CKA暴跌）和像素级保真度（残差暴涨）**。这说明冻结 probe 阶段的最佳表征，**不是“最像教师”的表征**，而是经过学生侧适配器重排后**更适合 R-ASPP head 线性解码**的表征。这也解释了为什么 A5 的 probe 最强，但端到端微调（FT）后却被 A0 反超——过度适配 probe head 的表征在完全解冻训练时可能不够鲁棒。
3. A2 的 OS=16 残差高达 **0.972**（远超 A0 的 0.444），CKA 降至 0.502；尽管 OS=4/8 的 CKA 保持正常（0.767/0.607），但其最终 mIoU 仅 0.235。
教师侧 adapter 的可训练虽然能让特征损失下降，但会**彻底瓦解高层（OS=16）教师目标的空间锚点**（残差接近 1.0 意味着重建完全失效）。由于分割极度依赖高层语义（OS=16），高层的崩塌直接拖垮了整体性能。这证明**固定目标侧（教师侧）投影是必要的正则项**，不能让学生去“污染”教师的固定坐标。
4. **A3（随机正交）**：浅层 OS=4/8 的 CKA（0.532/0.410）远低于 A0（0.761/0.623），残差（0.620/0.772）也显著更高。说明**随机低秩子空间完全无法捕捉教师浅层的细粒度结构**，PCA 主方向对于浅层空间对齐至关重要。
5. **A4（无标准化）**：OS=4/8 的残差超过 **1.0**（1.065/1.078），且 CKA 跌至全表最低（0.408/0.381）。说明不做 StandardScaler 时，通道量级差异（尤其是浅层）会严重扭曲主成分方向，导致大量不可逆的信息丢失。**标准化的收益主要集中在浅层（OS=4/8）**，这与第 2 节中 OS=4 累计解释方差仅 61.9% 相呼应——浅层特征本身高度冗余且尺度不一，必须标准化才能有效压缩

### b. A0/A1/A5 端到端微调

FT 从相应 probe 最优 checkpoint 出发，解冻 backbone+head，执行 80k 步端到端 CE。三组目前均为 `seed=42`，不能用单 seed 差值声明 FT 方案之间存在统计显著差异。

| **实验** | **初始化** | **最优 step** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** | **相对 S2-0 均值** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **A0-FT** | A0 best probe | 80000 | **0.56395** | **0.64443** | **0.92408** | **0.46185** | **0.46461** | **+0.06830** |
| A1-FT | A1 best probe | 75129 | `0.55159` | `0.62378` | `0.91310` | `0.44665` | `0.45110` | `+0.05594` |
| A5-FT | A5 best probe | 75129 | `0.54890` | `0.62326` | `0.91211` | `0.44678` | `0.45234` | `+0.05325` |
| **S2-0** | scratch | — | `0.49565 ± 0.00522` | `0.57649 ± 0.00861` | `0.88647 ± 0.00292` | `0.39144 ± 0.00810` | `0.38299 ± 0.00373` | — |

三组 FT 均明显超过 S2-0 三 seed 均值，并同时改善 small-object mIoU 与 boundary F1。这说明教师特征预训练得到的 backbone 不是只能被线性 probe 解码；在监督端到端适配后，它为 Cityscapes 分割提供了优于 scratch 的初始化。

A0-FT 比 A1-FT 高 `0.01236`，比 A5-FT 高 `0.01505`。A0/A1 的投影函数虽然数值等价，但首轮 probe checkpoint 的训练轨迹和 head 权重并不完全相同，FT 后差异可能被放大。A5 的 probe 最强并未转化为最强 FT，说明冻结表征的线性可解码性和端到端可适应性是两个不同指标。

## 1.5 实验结论

### **1. 固定目标优于可学习教师侧目标**

A2 的 OS=16 对齐残差达到 0.972，probe mIoU 仅 0.23590。教师侧 adapter 与学生共同优化会削弱固定表征锚点，即使 feature loss 可以下降，也不保证得到适合语义 probe 的 backbone。

### **2. PCA 主方向和 StandardScaler 均有作用**

A3 相对 A0 下降 0.078，且 OS=4/8 CKA 明显降低，说明任意随机低秩子空间不能替代 PCA 主方向。A4 相对 A0 小幅下降，表明 StandardScaler 的贡献小于 PCA 主方向，但对浅层通道尺度校准仍有价值。

### **3. 低维教师空间优于完整教师空间**

A6 三 seed mIoU 仅 `0.27400 ± 0.00157`，稳定低于 A0/A5。将学生扩张到完整教师通道空间增加了优化难度；固定低秩目标是当前 V2 学生更合理的蒸馏接口。

# 2.  K组实验——响应蒸馏实验

## 2.1 实验目标

在真实监督分割训练中，教师的中间表征和像素响应分别提供多少增量，二者是否互补：

1. `K1-K0`：没有教师 logits 时，中间 feature KD 是否优于纯 CE；
2. `K2-K0`：没有 feature KD 时，pixel-logit KD 是否优于纯 CE；
3. `K3-K1`：已有 feature KD 后，教师 logits 是否仍有增量；
4. `K3-K2`：已有 logits KD 后，feature KD 是否仍提供额外信息。

| **编号** | **硬标签 CE** | **A0 feature KD** | **T1 logits KD** | **目的** |
| --- | --- | --- | --- | --- |
| K0 | 是 | 否 | 否 | 受控 CE-only 基线 |
| K1 | 是 | 是 | 否 | 测量中间 feature KD 的独立增量 |
| K2 | 是 | 否 | 是 | 测量 pixel-logit KD 的独立增量 |
| K3 | 是 | 是 | 是 | 测量两种知识的互补性 |

## 2.2 实验方法

### a. feature KD / **A0 固定特征损失（K1/K3）**

对三层特征逐层计算 MSE，然后取算术平均：

$$

L_{feat}=\frac{1}{3}\sum_{l\in\{4,8,16\}}
\operatorname{mean}_{B,C,H,W}\left(f_s^l-\hat f_t^l\right)^2

$$

1. **逐元素相减**：对同一空间位置的每个通道像素，计算学生特征 $f_s$与投影后教师特征 $\hat f_t$ 的差值（形状保持 `[B, C, H, W]`）；
2. **逐元素平方**：对差值张量中的每一个数求平方（保持 `[B, C, H, W]`）；
3. **全局平均**：对这个平方后的张量，**一次性**除以总元素个数 `(B × C × H × W)`，得到一个**标量**损失值；
4. **三层等权**：将 OS=4、8、16 三层得到的标量损失相加后 **除以 3**。
- 计算时 **不使用标签 mask**，不按类别或像素有效性筛选
- 该损失仅在 **K1 和 K3** 中启用（K0/K2 不计算），并配合 warm-up（前 4000 step 线性从 0 增至 1）

### b. logits KD / **全分辨率 masked logits KL（K2/K3）**

教师和学生都通过各自完整 R-ASPP 前向得到  [B,19,H,W]  logits。

- R-ASPP 检测头
    - **输入**：来自骨干网络（如MobileNetV2的`backbone.18`层）的特征图，形状为 `[B, 1280, 32, 64]`。这里的 `1280` 是通道数，`32 x 64` 是特征图的高和宽（相对于输入 `512 x 1024` 的图像，步长为16）。
    - **核心处理**：这个阶段通常由两条并行的路径组成，用以捕获不同尺度的上下文信息。
        - **路径一（主路径）**：一个 `1x1` 卷积层，将输入通道数（如1280）压缩到一个中间通道数（如128），随后接批归一化（BN）和ReLU激活函数。
        - **路径二（全局路径）**：一个全局平均池化层（`AdaptiveAvgPool2d(1)`），将整个特征图压缩成 `[B, 1280, 1, 1]` 的向量，以捕捉全局上下文信息。然后通过一个 `1x1` 卷积和Sigmoid函数生成一个注意力门（gate）。
    - **特征融合**：处理完的特征会被融合。一种常见的方式是将路径一的输出与路径二生成的注意力门进行逐元素相乘（`out_high * gate`），从而让模型关注更重要的全局信息。
    - **分类与输出**：融合后的特征会被送入一个 `1x1` 卷积分类器（`high_classifier`），将通道数映射到目标类别数（如19），从而得到最终的logits。在K组实验中，这个logits的形状是 `[B, 19, 32, 64]`，即**“OS=16 logits”**。这些 [B, 19, 32, 64] 的logits会**被双线性上采样**回输入图像的分辨率 [B, 19, 512, 1024]，用于计算最终的 CE损失和 KL损失。

固定 $T = 4 , \lambda_{logit} =0.5$

定义：

$p_t=softmax(z_t/T),\qquad \log p_s=log\_softmax(z_s/T),$

$L_{logit}=T^2\cdot\frac{1}{|V|}
\sum_{(b,h,w)\in V}\sum_{c=1}^{19}
p_{t,c}\left(\log p_{t,c}-\log p_{s,c}\right).$

## 2.3 实验内容

| **阶段** | **编号** | **模型/目标** | **本次实验的唯一要求** | **完成条件与产物** |
| --- | --- | --- | --- | --- |
| 知识 | K0 | V2 监督基线 | `L=L_seg`，受控重跑 S2-0 | 3 seed；与 K1-K3 共用初始化和 batch 顺序 |
| 知识 | K1 | 仅 feature KD | `L=L_seg+lambda_feat*L_feat`，使用唯一 `A_best`，不用 logits | 3 seed；报告 `K1-K0` |
| 知识 | K2 | 仅 logits KD | `L=L_seg+lambda_logit*L_logit`，教师 head 已训练并冻结 | 3 seed；报告 `K2-K0` |
| 知识 | K3 | feature+logits KD | 合并 K1/K2，权重不变 | 3 seed；报告 `K3-K1` 和 `K3-K2` |
| 知识 | K3-G | K3 梯度夹角审计 | 不改变 K3 的初始化、训练目标或权重；重跑 `seed=42`，在预注册的固定 batch 和 step 上记录 CE/feature/logits 对 OS=4/8/16 学生特征的两两 gradient cosine | 跑满 80k；覆盖 warm-up 结束、中期和后期；输出各层 `cos(CE,feat)`、`cos(CE,logit)`、`cos(feat,logit)`，区分目标冲突与 feature 梯度尺度过小 |
| 知识 | K4 | A0 初始化 + logits KD | 从 A0 `seed=42` 最优 probe checkpoint（backbone+head）出发，按 A0-FT 的 80k CE 微调协议训练；唯一新增项为 K2 的 logits KD（`T=4`、`lambda_logit=0.5`、前 4000 step warm-up），不使用 feature KD | 首轮 seed 42；报告 `K4-A0-FT` 和 `K4-K2`；若相对 A0-FT 的增益小于当前 K 组 seed 波动则停止，否则扩展 3 seed |

## 2.4 实验结果

### a. 主指标汇总

下表标准差为样本标准差（ddof=1，N=3）。

| **实验** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| K0 | `0.50694 ± 0.00308` | `0.58778 ± 0.00724` | `0.88934 ± 0.00661` | `0.40246 ± 0.00098` | `0.38683 ± 0.00828` |
| K1 | `0.52203 ± 0.00219` | `0.59546 ± 0.00760` | `0.91173 ± 0.00651` | `0.40978 ± 0.00851` | `0.43482 ± 0.00613` |
| **K2** | **0.55193 ± 0.00425** | **0.63288 ± 0.02107** | `0.92078 ± 0.00552` | **0.45279 ± 0.01513** | `0.42467 ± 0.00775` |
| K3 | `0.54975 ± 0.00403` | `0.61874 ± 0.00080` | **0.92434 ± 0.00303** | `0.44361 ± 0.00586` | **0.44203 ± 0.00165** |
| A0-FT（外部协议参考） | `0.55215 ± 0.01022` | `0.63121 ± 0.01170` | `0.91991 ± 0.00395` | `0.45261 ± 0.01245` | `0.45307 ± 0.01477` |
| **K4（A0 初始化+logits KD）** | **0.57049 ± 0.00582** | **0.64207 ± 0.01410** | **0.93001 ± 0.00256** | **0.47086 ± 0.02522** | **0.46027 ± 0.00281** |

在 scratch 2×2 内：

- K2 的平均 mAcc 和 small-object mIoU 最高
- K3 的 pixel accuracy 最高且 boundary F1 的 seed 波动最小；
- A0-FT 与 K2/K3 的 mIoU 均值接近，但 seed 波动更大。
- K4 使用不同的分阶段初始化协议，不能作为 scratch 2×2 的第五个单元，但其 mIoU、mAcc、pixel accuracy 和 small-object mIoU 均值均为当前表中最高，boundary F1 也与 A0-FT 接近，因此是当前最佳 V2 候选。

### b. 预注册配对差值

paired bootstrap 对每个 seed 的 445 张相同 dev 图像进行成对重采样，每次先聚合 19×19 confusion matrix 再计算 mIoU；使用 100,000 次重采样。

| **对比** | **三个 seed 的 mIoU 差值** | **平均差值** | **每 seed 95% CI 结论** |
| --- | --- | --- | --- |
| K1-K0 | `+0.01165/+0.01499/+0.01863` | `+0.01509` | 3/3 完全高于 0；区间端点总范围 `[+0.00089,+0.02750]` |
| K2-K0 | `+0.04024/+0.05185/+0.04288` | `+0.04499` | 3/3 完全高于 0；区间端点总范围 `[+0.03020,+0.06231]` |
| K3-K1 | `+0.03187/+0.02949/+0.02179` | `+0.02772` | 3/3 完全高于 0；区间端点总范围 `[+0.01306,+0.04172]` |
| K3-K2 | `+0.00328/-0.00736/-0.00246` | `-0.00218` | 0/3 排除 0；区间端点总范围 `[-0.01951,+0.01095]` |

因此，K1 和 K2 相对 K0 的独立收益均成立，且 logits 在 feature KD 之外仍有明确增量；反方向不成立，即当前 feature KD 没有在 logits KD 之上提供可测的 mIoU 增量。

## 2.5 实验结论

### 1. **响应蒸馏是 scratch 端到端训练中的主要增益来源**

K2 三 seed mIoU 为 `0.55193 ± 0.00425`，相对 K0 的配对增益为 `+0.04024/+0.05185/+0.04288`；三个 seed 的逐图 paired-bootstrap 95% CI 均完全高于 0。

### 2. **特征蒸馏单独使用也有效，但增益明显小于响应蒸馏**

K1 mIoU 为 `0.52203 ± 0.00219`，相对 K0 的平均增益为 `+0.01509`，三个 seed 的 paired-bootstrap 95% CI 同样均高于 0。因此不能把 K1 解释为无效，只能说其任务指标增益较弱。

### 3. **K3 与 K2 的 mIoU 性能相近，未证明两种知识在主指标上可加**

K3 mIoU 为 `0.54975 ± 0.00403`，相对 K2 的配对差值为 `+0.00328/-0.00736/-0.00246`，三次 95% CI 均跨 0。主指标排序应写为 `K2≈K3>K1>K0`，不应按单个 seed 宣称 K3 胜出。

K3 相比 K2 的平均 boundary F1 提高 `+0.01736`，pixel accuracy 提高 `+0.00356`，但 mAcc 降低 `-0.01414`、small-object mIoU 降低 `-0.00919`。特征项更像改变了边界与类别均衡之间的取舍，而不是提供统一的 mIoU 增益。

### 4. **K4 在 A0 基础上进行微调效果最佳，成为最佳 MNv2 候选之一**

K4 三 seed mIoU 为 `0.56818/0.56618/0.57710`，汇总为 `0.57049 ± 0.00582`；相对 A0-FT 的增量 `Delta_s` 为 `+0.00423/+0.01981/+0.03095`，三个 seed 均为正。

# 3.  R组实验 —— 关系蒸馏实验

## 3.1 实验目标

1. `R1`：同一 batch 内不同图像之间的关系是否提供额外信息；
2. `R2`：同一图像不同空间位置之间的关系是否提供额外信息；
3. `R3`：跨图像关系和图内空间关系是否互补；
4. `R4`：去除逐点 feature KD 后，关系约束是否仍能单独提供可用监督。

| **编号** | **硬标签 CE** | **A0 feature KD** | **R1 跨图像关系** | **R2 图内空间关系** | **目的** |
| --- | --- | --- | --- | --- | --- |
| R0 | 是 | 是 | 否 | 否 | K1 受控复现 |
| R1 | 是 | 是 | 是 | 否 | 测量 batch 内图像关系增量 |
| R2 | 是 | 是 | 否 | 是 | 测量图内空间关系增量 |
| R3 | 是 | 是 | 是 | 是 | 测量两类关系是否互补 |
| R4 | 是 | 否 | 选定一项 | 选定一项 | relation-only 欠约束诊断 |

## 3.2 实验方法

关系项在 OS=4/8/16 分别计算并等权平均：

$$
L_{rel}=\frac{1}{3}\sum_{l\in\{4,8,16\}}L_{rel}^l.
$$

### a.  **R1：跨图像 batch 关系**

#### 步骤一：计算每张图像的掩码全局平均池化（Masked GAP）

对物理批次中的**每一张图像**（`B=4`），在 OS=4/8/16 三个特征层上分别操作：

1. **生成空间掩码**：将标签中的有效像素（`target != 255`）通过 **nearest-neighbor** 下采样到当前特征图分辨率（如 `32×64`），得到 `m_b`。
2. **掩码平均**：只对掩码内的有效像素取通道维度的平均值，得到该图像的全局特征向量。
    - 学生得到  $g_{s,b}^l$ ，教师得到 $g_{t,b}^l$ （形状均为 `[C]`）。
    - **关键**：`ignore` 像素（如 255）**完全不参与**分子和分母的计算。

---

#### 步骤二：构造跨图像余弦相似度矩阵

将当前物理 batch 中的 4 张图的向量堆叠在一起（形状 `[4, C]`），沿通道维进行 L2 归一化，然后计算**自乘矩阵**（即两两余弦相似度）：

- 学生矩阵： $M_s^l = q_s^l \cdot (q_s^l)^T$ ，形状为 `[4, 4]`。
- 教师矩阵：$M_t^l = q_t^l \cdot (q_t^l)^T$ ，形状为 `[4, 4]`。

这个矩阵的第 $(i,j)$ 个元素，代表第 $i$  张图和第  $j$ 张图全局特征的相似性（**包含对角线**，即图与自身的相似度，通常接近 1）。

---

#### 步骤三：计算逐层 MSE 并等权平均

对 OS=4、8、16 三个层分别计算 MSE，然后取平均：

$$
L_{R1}^l = \frac{1}{B^2} \sum_{i=1}^{4} \sum_{j=1}^{4} \left( M_{s,ij}^l - M_{t,ij}^l \right)^2
$$

$$

L_{R1} = \frac{1}{3} \left( L_{R1}^{os4} + L_{R1}^{os8} + L_{R1}^{os16} \right)
$$

### b.  **R2：图内空间关系**

#### 步骤一：通过 Masked Pooling 将特征图压为 128 个 Token

对于物理批次中的**每一张图像**（`B=4`），在 OS=4/8/16 三个特征层上分别操作：

1. **划分空间网格**：将当前特征图（如 `32×64`）通过自适应平均池化（Adaptive Avg Pool）固定划分为 **`8 × 16 = 128`** 个空间格子（bin）。
2. **掩码平均（关键）**：对于每个格子，只取原始标签中**有效像素（非 ignore）** 的特征做平均。
    - 如果该格子内全是 `ignore`（如 255），则该 token 被标记为 **`invalid`（无效）**，不参与后续计算。
    - 最终每张图、每层得到 `[128, C]` 的 token 集合。

---

#### 步骤二：构造图内空间相似度矩阵

将上一步得到的 128 个 token 沿通道维进行 L2 归一化，然后计算**自乘矩阵**（即两两余弦相似度）：

- 学生矩阵：$A_{s,b}^l = \tilde X_{s,b}^l \cdot (\tilde X_{s,b}^l)^T$ ，形状为 `[128, 128]`。
- 教师矩阵： $A_{t,b}^l = \tilde X_{t,b}^l \cdot (\tilde X_{t,b}^l)^T$ ，形状为 `[128, 128]`。

这个矩阵的第 $(p,q)$  个元素，代表**同一张图内，第  $p$ 个空间区域和第 $q$ 个空间区域的全局特征相似度**（包含对角线，即区域与自身的相似度）。

---

#### 步骤三：只对“有效 Pair”计算 MSE 并等权平均

这是 R2 最特殊的地方：**不是所有 128×128 个 Pair 都算**，必须两个 token 都有效（即对应的空间格子内都有有效像素）才计入损失。

**生成有效对掩码**：$V_{b,p,q} = v_{b,p} \cdot v_{b,q}$（只有双方都 valid，该位置才为 1）。

计算逐层损失（只在 V=1 的位置求平均）：

$$
L_{R2}^l = \frac{\sum_{b,p,q} V_{b,p,q} \left( A_{s,b,pq}^l - A_{t,b,pq}^l \right)^2}{\sum_{b,p,q} V_{b,p,q}}
$$

**三层等权平均**：

$$

L_{R2} = \frac{1}{3} \left( L_{R2}^{os4} + L_{R2}^{os8} + L_{R2}^{os16} \right)
$$

## 3.3 实验内容

| **阶段** | **编号** | **模型/目标** | **本次实验的唯一要求** | **完成条件与产物** |
| --- | --- | --- | --- | --- |
| 关系 | R0 | K1 复现 | CE+`A_best` feature KD，无关系项 | seed 42；排除代码分支差异 |
| 关系 | R1 | 图像关系 | R0+masked GAP 后的 batch `B×B` cosine matrix MSE | seed 42；记录关系梯度和 batch 大小 |
| 关系 | R2 | 空间关系 | R0+池化至 8×16 后的 `128×128` token 关系 | seed 42；重点报告边界/小目标和显存 |
| 关系 | R3 | 两种关系 | R0+R1+R2；各自 `lambda_rel=0.03` 起步并 warm-up | seed 42；关系梯度为 feature 梯度的 5%–20% |
| 关系 | R4 | relation-only | CE+关系项，移除逐点 feature MSE | 1 seed，仅诊断欠约束，不作主候选 |
| 关系 | R5 | feature+relation+logits | 在 R2（`lambda_r2=0.3`）基础上加入 K 组已锁定的 pixel-logit KD（`T=4`、`lambda_logit=0.5`、前 4000 step warm-up）；保留 `lambda_feat=1.0`，不加入 R1 | 首轮 seed 42；比较 R5-R2 的 logits 增量和 R5-K3 的空间关系增量；先通过 mIoU、paired-bootstrap 和梯度门后再扩 3 seed |

## 3.4 实验结果

### 1a. 主要指标 seed = 42 结果

| **实验** | **seed** | **最优 step** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R0 | 42 | 75129 | `0.522120` | `0.589288` | `0.918958` | `0.408331` | `0.441898` |
| R1 | 42 | 80000 | `0.528969` | `0.599473` | `0.906953` | `0.416227` | `0.440011` |
| R2-low (`lambda=0.03`) | 42 | 75129 | `0.528936` | `0.597511` | **0.921213** | `0.409646` | **0.445285** |
| **R2 (`lambda=0.3`)** | 42 | 75129 | **0.539689** | **0.607588** | `0.917864` | **0.440110** | `0.441265` |
| R3 | 42 | 75129 | `0.532366` | `0.603945` | `0.911745` | `0.435482` | `0.433973` |
| R4 | 42 | 75129 | `0.525697` | `0.602368` | `0.910002` | `0.423396` | `0.401664` |
| **R5** | 42 | 80000 | **0.573653** | **0.653850** | **0.929604** | **0.489157** | **0.456676** |

R2 `lambda=0.3` 在主指标、mAcc 和 small-object mIoU 上最高，但不是所有指标都最高；R2-low 的 pixel accuracy 和 boundary F1 最高。

### 1b. R2/R3 3 seed 扩展结果

标准差为样本标准差（ddof=1）。R2 和 R3 均实际跑满 80k，表中的 best step 只是 dev 选择结果。

| **实验** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| R2 `lambda=0.3` | **0.53275 ± 0.00601** | **0.60903 ± 0.00354** | **0.91060 ± 0.00646** | **0.43126 ± 0.00785** | **0.43399 ± 0.00970** |
| R3 `R1=0.03/R2=0.3` | `0.52839 ± 0.00347` | `0.60391 ± 0.00849` | `0.90704 ± 0.01008` | `0.42690 ± 0.00764` | `0.42992 ± 0.01342` |

R3 相对 R2 的 mIoU 差值为 `-0.007323/-0.003103/-0.002651`，三个 seed 均为负；因此当前 R1+R2 组合没有显示出主指标互补。

### 2a. seed = 42 逐图配对统计

对 445 张相同 dev 图像进行 100,000 次 paired bootstrap；每次成对重采样图像，先聚合 `19x19` confusion matrix，再计算 mIoU。随机种子固定为 `260820`。

| **对比** | **checkpoint mIoU 差值** | **bootstrap 平均差值** | **95% CI** | **解释** |
| --- | --- | --- | --- | --- |
| R1-R0 | `+0.006849` | `+0.006732` | `[-0.001215,+0.015468]` | 跨 0；单 seed 图像层证据不足 |
| R2-R0 | `+0.017569` | `+0.017315` | `[+0.007930,+0.027524]` | 完全高于 0 |
| R3-R0 | `+0.010246` | `+0.010017` | `[+0.001409,+0.019169]` | 完全高于 0 |
| R4-R0 | `+0.003577` | `+0.003175` | `[-0.005518,+0.012032]` | 跨 0；relation-only 增益证据不足 |
| R3-R2 | `-0.007323` | `-0.007298` | `[-0.013784,-0.000867]` | 当前 seed 下 R3 低于 R2 |
| R2-R4 | `+0.013992` | `+0.014141` | `[+0.007002,+0.021484]` | 当前 seed 下保留 feature KD 的方案更高 |

### 2b. **R2/R3  3 seed paired-bootstrap**

对每个 seed 的 R2/R3 与同 seed K1 进行 100,000 次逐图 paired bootstrap。结果为：

| **seed** | **R2-K1 平均差值** | **R2-K1 95% CI** | **R3-K1 平均差值** | **R3-K1 95% CI** | **R3-R2 平均差值** | **R3-R2 95% CI** |
| --- | --- | --- | --- | --- | --- | --- |
| 42 | `+0.017317` | `[+0.007853,+0.027520]` | `+0.010014` | `[+0.001414,+0.019124]` | `-0.007303` | `[-0.013796,-0.000847]` |
| 3407 | `+0.009487` | `[+0.002485,+0.016859]` | `+0.006258` | `[+0.000022,+0.012558]` | `-0.003229` | `[-0.010095,+0.003142]` |
| 260805 | `+0.005167` | `[-0.002582,+0.012796]` | `+0.002512` | `[-0.005191,+0.010446]` | `-0.002655` | `[-0.008854,+0.003691]` |

逐图 CI 显示 R2 相对 K1 的方向三 seed 一致，但只有 seed=42/3407 的图像重采样 CI 完全高于 0.

R3-R2 三次训练差值均为负，但后两个 seed 的逐图 CI 跨 0.

### 3. R2/R4 **支持 feature MSE 的辅助作用**

| **指标** | **R2：`CE+feature+R2`** | **R4：`CE+R2`** | **R2-R4** |
| --- | --- | --- | --- |
| mIoU | `0.539689` | `0.525697` | `+0.013992` |
| mAcc | `0.607588` | `0.602368` | `+0.005220` |
| pixel accuracy | `0.917864` | `0.910002` | `+0.007862` |
| small-object mIoU | `0.440110` | `0.423396` | `+0.016714` |
| boundary F1 | `0.441265` | `0.401664` | `+0.039601` |
| final raw R2 loss | `0.16538` | `0.01338` | `+0.15200` |

R4 更容易把关系矩阵拟合到很低残差，却在所有五项 dev 指标上低于 R2，尤其边界指标差距最大。最合理的解释是：feature MSE 提供逐点表示锚点，防止学生仅通过一种对关系矩阵友好、但对像素分类不充分的表示来降低 R2；二者联合形成更受约束的折中解

### 4. R5：**feature、relation 与 logits 三者结合**

| **对比** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| R5 `seed=42` | `0.573653` | `0.653850` | `0.929604` | `0.489157` | `0.456676` |
| R5-R2 `seed=42` | `+0.033964` | `+0.046261` | `+0.011740` | `+0.049048` | `+0.015411` |
| R5-K3 `seed=42` | `+0.019663` | `+0.035630` | `+0.001854` | `+0.039875` | `+0.012703` |

K 组已显示 K3≈K2，即 feature MSE 没有在 logits KD 上提供可测 mIoU 增量；R 组又显示 R3<R2，即 R1 没有在 R2 上提供增量。R5 的单 seed 结果说明 logits KD 在 `feature+R2` 之上仍有明显增量，**但这不等于三类知识的收益可以线性相加**。

## 3.5 实验结论

### 1. **R1 的跨图关系约束弱于 R2 的图内关系约束**

 R1 相对 R0 的 mIoU 增量为 `+0.006849`，但 paired-bootstrap 95% CI `[-0.001215,+0.015468]` 跨 0；其后期 relation/feature 有效梯度比例多低于预设 `0.05` 下限。相反，R2 `lambda=0.3` 在大部分固定审计点进入 `0.05-0.20` 目标带，并获得更大的 mIoU 和小目标增益。

### 2. **R1 与 R2 在3 seed 上未显示可加性**

R3 mIoU 为 `0.52839 ± 0.00347`，R2 为 `0.53275 ± 0.00601`；R3-R2 的 mIoU 差值为 `-0.007323/-0.003103/-0.002651`，三次均为负，均值 `-0.004359`。因此在当前 `R1=0.03/R2=0.3` 配置下，两类关系没有显示主指标互补。

### 3. **R2/R4 支持 feature MSE 对关系蒸馏具有辅助作用，但不能把全部差值归因于纯协同增益**

 R4 移除 feature MSE 后，mIoU 从 R2 的 `0.539689` 降至 `0.525697`，差值 `-0.013992`；boundary F1 下降 `-0.039601`。同时 R4 raw R2 loss 最终降至 `0.0134`，而 R2 保持在 `0.1654`，说明 relation-only 更容易得到低关系残差，却没有转化为更高分割性能。严格的“交互量”仍需要 CE-only、feature-only、relation-only、feature+relation 的同组三 seed 2×2 对照。

### 4. **R5 `seed=42` 显示 feature+R2+logits 的组合具有很强的单 seed 增益，但仍不能作为稳定结论**

R5 mIoU 为 `0.573653`，相对同 seed R2 增加 `+0.033964`，相对同 seed K3 增加 `+0.019663`；逐图 paired-bootstrap 的 R5-R2 95% CI 为 `[+0.025654,+0.042453]`。R5 还高于 K4 三 seed均值 `0.57049`，但这是 R5 单 seed 对 K4 三 seed均值的非配对、协议不同比较，不能宣称 R5 已超过 K4。

# 4. D组实验 —— 分布约束蒸馏实验

## 4.1 实验目标

1. `D1`：教师/学生 feature 的一阶、二阶统计对齐是否提供超出 pointwise feature MSE 的信息；
2. `D2`：基于随机投影的全局分布距离是否提供增量；

## 4.2 实验方法

### 提取有效 token 并标准化

- 对每一层 $l \in \{\text{os4},\text{os8},\text{os16}\}$，在固定 A0 投影后的学生特征 $f_s^l$ 和教师特征 $\hat f_t^l$ 上，仅收集**有效像素**（非 ignore）对应的 token。
- 每层当前有效 token 矩阵记为 $X_s^l \in \mathbb{R}^{N_l \times C_l}$（学生）和  $X_t^l \in \mathbb{R}^{N_l \times C_l}$（教师），其中 $N_l$ 是有效 token 数，$C_l$ 是投影后的通道数。
- **特征归一化**：使用教师 batch 统计量对两者进行标准化（见 5.2）：
    
    $$
    x' = \frac{x - \mu_{t,l}}{\sigma_{t,l} + \epsilon},
    
    $$
    
    其中 $\mu_{t,l}$, $\sigma_{t,l}$ 由当前 batch 教师 token 计算并 detach，$\epsilon=10^{-6}$。学生也使用**同一教师统计量**，避免各自标准化后抹除分布差异。
    

### a. D1 : CORAL 损失

**计算均值和协方差**

- 均值
    
    $$
    
    \mu_s^l = \frac{1}{N_l}\sum_{i=1}^{N_l} X_{s,i}^l,\qquad
    \mu_t^l = \frac{1}{N_l}\sum_{i=1}^{N_l} X_{t,i}^l.
    
    $$
    
- 协方差（无偏估计，分母为 $\max(N_l-1,1)$）：
    
    $$
    
    C_s^l = \frac{(X_s^l - \mu_s^l)^T (X_s^l - \mu_s^l)}{\max(N_l-1,1)},\qquad
    C_t^l = \frac{(X_t^l - \mu_t^l)^T (X_t^l - \mu_t^l)}{\max(N_l-1,1)}.
    
    $$
    
- 教师均值、中心化 token 和协方差必须 **detach**，不传播梯度；学生统计保留梯度。

---

**CORAL 损失公式**

对每一层计算：

$$

L_{\text{CORAL}}^l =
\frac{1}{C_l^2} \|C_s^l - C_t^l\|_F^2 + \frac{1}{C_l} \|\mu_s^l - \mu_t^l\|_2^2.

$$

- 第一项为协方差差值的 **Frobenius 范数平方**，除以通道数平方做尺度归一化；
- 第二项为均值差值的 **L2 范数平方**，除以通道数。

最终层间聚合为**三层等权平均**：

$$

L_{\text{CORAL}} = \frac{1}{3} \sum_{l \in \{\text{os4},\text{os8},\text{os16}\}} L_{\text{CORAL}}^l.

$$

### b. D2 : SWD 损失

**固定随机方向（切片）生成**

- 每层独立生成 **64 个**固定高斯随机方向，方向数量固定为 `num_slices = 64`。
- 每个方向向量 $r_l^k \in \mathbb{R}^{C_l}$ 生成后做 **L2 归一化**。

---

**投影到一维空间**

对每个方向 $k = 1, \dots, K$ $(K=64)$，将标准化后的 token 矩阵投影到该方向：

$$
u_s^{l,k} = X_s^l \cdot r_l^k \in \mathbb{R}^{N_l},\qquad
u_t^{l,k} = X_t^l \cdot r_l^k \in \mathbb{R}^{N_l}.
$$

---

**排序与 L1 距离计算**

对每个方向，分别对投影值进行**升序排序**，然后计算排序后向量之间的 **L1 距离**（即绝对差之和），再除以 token 数 $N_l$ 和方向数 $K$ 进行平均：

$$
L_{\text{SWD}}^l =
\frac{1}{K \cdot N_l}
\sum_{k=1}^{K}
\left\|
\text{sort}(u_s^{l,k}) - \text{sort}(u_t^{l,k})
\right\|_1.
$$

- **排序操作必须在 FP32 中执行**，以避免低精度下的排序误差。
- 若 $N_s = N_t$（同一 batch 内有效 token 数相同），直接按上式在本地计算。

---

**层间聚合**

最终 SWD 损失为三层（OS=4/8/16）的**等权平均**：

$$
L_{\text{SWD}} = \frac{1}{3} \sum_{l \in \{\text{os4},\text{os8},\text{os16}\}} L_{\text{SWD}}^l.
$$

## 4.3 实验内容

| **阶段** | **编号** | **模型/目标** | **本次实验的唯一要求** | **完成条件与产物** |
| --- | --- | --- | --- | --- |
| 分布 | D0 | K1 复现 | 无新增分布项 | seed 42；分布实验锚点 |
| 分布 | D1 | CORAL | K1+共同瓶颈均值/协方差对齐 | seed 42；有效才扩 3 seed |
| 分布评估 | D1-PB | D1 paired-bootstrap | 不训练模型；对 D1 与同 seed K1 的 445 张逐图 confusion matrix 做配对重采样，评估 mIoU 差值不确定性 | 使用 `seed=42/3407/260805`；每个 seed 100,000 次重采样；输出差值分布、95% CI 和汇总 JSON |
| 分布 | D2 | SWD | K1+SWD | seed 42；记录带宽或投影数 |

## 4.4 实验结果

### 1a.主要指标

| **实验** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| K1 anchor | `0.522032 ± 0.002194` | `0.595458 ± 0.007600` | `0.911730 ± 0.006515` | `0.409779 ± 0.008514` | `0.434824 ± 0.006129` |
| D1 CORAL | **0.526454 ± 0.006117** | **0.598474 ± 0.004390** | **0.912531 ± 0.007435** | **0.414889 ± 0.011564** | **0.436389 ± 0.010864** |
| D2 SWD | `0.520336 ± 0.007948` | `0.596087 ± 0.004896` | `0.901373 ± 0.007520` | `0.411833 ± 0.014585` | `0.426451 ± 0.008397` |

### 1b. 相对同 seed K1 的差值

| **实验** | **seed=42** | **seed=3407** | **seed=260805** | **均值 ± 样本标准差** |
| --- | --- | --- | --- | --- |
| D1 mIoU-K1 | `+0.010911` | `+0.001138` | `+0.001216` | **`+0.004422 ± 0.005620`** |
| D2 mIoU-K1 | `+0.007394` | `-0.004076` | `-0.008406` | **`-0.001696 ± 0.008164`** |

D1 三个 seed 均为正，但增益主要由 seed=42 拉高；D2 的两个新增 seed 均为负，说明 seed=42 的结果不具备代表性。D2 small-object mIoU 的三 seed 差值为 `+0.020322/+0.000612/-0.014773`，同样不稳定。

### 2. D1-PB 结果

| **seed** | **checkpoint delta** | **bootstrap delta mean** | **bootstrap delta std** | **95% CI** | **CI 是否排除 0** |
| --- | --- | --- | --- | --- | --- |
| 42 | `+0.010911` | `+0.010436` | `0.005168` | `[+0.000616,+0.020829]` | 是 |
| 3407 | `+0.001138` | `+0.001118` | `0.004327` | `[-0.007420,+0.009566]` | 否 |
| 260805 | `+0.001216` | `+0.001325` | `0.003397` | `[-0.005336,+0.008016]` | 否 |

每个 seed 均使用 bootstrap random seed `260820` 和 `100000` 次重复
D1-PB的结果表明 **D1 具有跨 seed 的同方向正向结果，但图像层证据有限**。（3 seed 中 2 seed 95% CI 跨0点，但 全部 seed 训练差值为正）

## 4.5 实验结论

### 1. **D1 CORAL 总体来看提升幅度不明显。**

**D1 CORAL 在训练 seed 层面保持正向，但 paired-bootstrap 只在 seed=42 上给出明确的图像层支持。**D1 三个 seed 相对 K1 的 mIoU 差值为 `+0.010911/+0.001138/+0.001216`，三次均为正，均值 `+0.004422 ± 0.005620`；D1 mIoU 为 `0.526454 ± 0.006117`。D1-PB 的 95% CI 分别为 `seed=42: [+0.000616,+0.020829]`、`seed=3407: [-0.007420,+0.009566]`、`seed=260805: [-0.005336,+0.008016]`。

### 2. D2 SWD 总体来看存在较大问题，不适合作为优化特征蒸馏的工具。

D2 相对 K1 的 mIoU 差值为 `+0.007394/-0.004076/-0.008406`，均值 `-0.001696 ± 0.008164`；两个新增 seed 均低于同 seed K1。D2 的 pixel accuracy 三次均下降，boundary F1 三次均下降，说明 seed=42 的 small-object 增益不能抵消整体稳定性问题。

### 2+. **D2 的问题很可能与 SWD 的有效梯度过强有关，但目前不能只归因于 lambda**

D2 的 SWD/feature 梯度比例在三个 seed 的固定审计点约为  0.30-0.93 ，持续高于预注册的  0.05-0.20  目标带；这与 D2 的高方差和后续 seed 退化相符。但这仍是机制证据，不是仅凭结果证明“唯一原因就是 lambda=0.1”。

# 5. H组实验 ——  激活函数改进

## 5.1 实验目标

1. `H1-H0`：把 Hardswish 替换到所有 inverted residual 激活位置，是否改变分割性能和部署效率；
2. `H2-H0`：只在 MobileNetV2 的 OS=16 后段替换 Hardswish，是否保留全量替换的精度收益并降低高分辨率激活开销；
3. `H3-H2`：在后段中只替换 depthwise convolution 后的激活，是否足以获得 Hardswish 的收益；
4. Hardswish 的收益究竟来自激活函数本身、深层位置，还是 inverted residual 内的具体放置位置。

## 5.2 实验方法

### Hardswish / h-swish 激活函数

MNv3上使用的，在深层网络替代 MNv2 的 ReLU6 的激活函数

swish（`x·sigmoid(x)`）在精度上优于ReLU，但sigmoid在移动端计算昂贵。V3提出了**h-swish（Hard Swish）**，用分段线性的ReLU6来近似sigmoid，公式为$x·ReLU6(x+3)/6$。这样既保留了swish的大部分精度收益，又避免了复杂的指数运算，并且对量化（INT8）非常友好。

## 5.3 实验内容

| **阶段** | **编号** | **模型/目标** | **本次实验的唯一要求** | **完成条件与产物** |
| --- | --- | --- | --- | --- |
| 激活 | H0 | V2 ReLU6 anchor | R5-compatible 协议；stem、inverted residual、final 1×1 和 R-ASPP 均保持原始 ReLU6 | seed 42；mIoU、paired bootstrap、FP32/INT8 延迟 |
| 激活 | H1 | V2 Hardswish 全 in-block | 只替换 `backbone.1..17` inverted residual 内所有 eligible `conv.0.2/conv.1.2`；stem/final/R-ASPP/linear bottleneck 不变 | seed 42；精度、梯度、延迟、导出和融合状态 |
| 激活 | H2 | V2 Hardswish 后段 | 只替换 `backbone.14..17` 内 expansion+depthwise 激活；固定 OS=16 后段主假设 | seed 42；与 H0/H1 做位置消融和效率比较 |
| 激活 | H3 | V2 Hardswish 后段 depthwise-only | 只替换 `backbone.14..17` 的 `conv.1.2` depthwise 激活；expansion 保持 ReLU6 | seed 42；区分 depthwise placement 与 expansion placement |

## 5.4 实验结果

以下均为最佳 `dev_local` checkpoint，总训练步数 80000 stpes

| **实验** | **best step** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- | --- |
| H0 | 75129 | `0.563093` | `0.637902` | `0.928136` | `0.465712` | `0.448784` |
| H1 | 55158 | `0.516091` | `0.592205` | `0.909467` | `0.407219` | `0.416976` |
| H2 | 75129 | `0.523697` | `0.587776` | `0.924252` | `0.377401` | `0.446227` |
| H3 | 55158 | `0.522735` | `0.585036` | `0.923480` | `0.395691` | `0.423684` |

相对同 seed H0 的描述性差值：

| **比较** | **mIoU** | **mAcc** | **pixel Acc** | **small-object mIoU** | **boundary F1** |
| --- | --- | --- | --- | --- | --- |
| H1-H0 | `-0.047002` | `-0.045697` | `-0.018669` | `-0.058493` | `-0.031807` |
| H2-H0 | `-0.039397` | `-0.050126` | `-0.003884` | `-0.088311` | `-0.002556` |
| H3-H0 | `-0.040358` | `-0.052867` | `-0.004656` | `-0.070021` | `-0.025099` |

H1/H3 的 best checkpoint 均在 step `55158`，最终 step=80000 的 dev mIoU 分别约为 `0.470511/0.503081`；H2 最终约为 `0.513372`。

这显示 H1/H3 存在后期 dev 退化，但不能仅凭训练曲线区分优化不稳定、泛化退化或 checkpoint 选择偶然性。

H2 的主要异常集中在稀有类别：`bus` IoU=`0.055652`、`train` IoU=`0.0000549`；H3 对应为 `0.183852/0.289575`，没有复现该类别崩溃。

*MNv3论文中，论文 Table 7 的 Cityscapes 分割结果反而说明，V3 的优势不能简单归因于 h-swish：在相同表格设置下，V2+R-ASPP 为 `72.84`，V3+R-ASPP 为 `72.64`；使用 LR-ASPP 后 V2 为 `72.97`，V3 为 `72.37`。论文报告的 V3 分割优势来自完整 backbone、通道调整、OS/atrous 设置和 LR-ASPP 等组合，而不是 h-swish 的独立因果效果。

## 5.5  实验结论

### **1. H1/H2/H3 在 seed=42 均低于 H0，更换激活函数存在明显退步。**

H1 的 mAcc、pixel accuracy、small-object mIoU 和 boundary F1 均下降；H2 的 small-object mIoU 下降 `0.088311`，且 `bus` IoU 为 `0.055652`、`train` IoU 约为 `5.5e-5`，出现明显稀有类崩溃。

### 2. **当前不继续 h-GELU/SiLU/GELU 激活网格**

paired-bootstrap 已进一步确认 H1/H2/H3 相对 H0 的图像层差异均为负，因此没有足够理由继续堆叠未登记激活；

# 6. 进一步研究

## 6.1 A/K/R/D/H 组实验中共性存在的问题

### 6.1.1 当前主要结果

| **实验** | **协议/seed** | **dev mIoU** | **small-object mIoU** | **当前允许的解释** |
| --- | --- | --- | --- | --- |
| T1 | 教师，3 seed | `0.776024 ± 0.002086` | `0.790174 ± 0.003894` | 教师参考上限，不是学生容量对照 |
| S2-0 | V2 scratch，3 seed | `0.495653 ± 0.005220` | `0.391439 ± 0.008099` | V2 端到端基线 |
| A0-FT | A0 初始化后纯 CE，3 seed | `0.55215 ± 0.01022` | `0.4526128 ± 0.0092` | 分阶段初始化的下游效果 |
| K4 | A0 初始化+logits KD，3 seed | `0.57049 ± 0.00582` | `0.47086 ± 0.02522` | 当前最强三 seed V2 候选 |
| R5 | feature+R2+logits，seed=42 | `0.573653` | `0.489157` | 单 seed 强筛选信号，不能与 K4 作稳定优劣结论 |
| H0 | ReLU6 anchor，3 seed | `0.560413 ± 0.002410` | `0.466036 ± 0.001349` | H 组内部 matched anchor |
| H1/H2/H3 | Hardswish，均 seed=42 | `0.516091/0.523697/0.522735` | `0.407219/0.377401/0.395691` | 负向筛选结果，不是三 seed 稳定排序 |

由此可以确认：蒸馏显著改善了 S2-0，但 K4/R5 的小目标指标仍明显低于 T1。

这个事实支持“**存在未解决的学生侧瓶颈**”。

### 6.1.2 提出假设

#### **猜测一：OS=16 与 R-ASPP 上采样伤害小目标**

在 `1024×2048` 输入上，OS=16 的主特征空间约为 `64×128`；一个小目标在该空间中可能只覆盖很少的 feature cell，经过 R-ASPP 和上采样后，边界和细粒度类别容易被平滑。训练 crop 为 `512×1024` 时对应空间约为 `32×64`，小目标还可能受到随机缩放和裁剪的进一步影响。因此，按目标在 feature map 上的覆盖面积、边界宽度和预测错误位置分析这一方向是合理的。

但“学生和教师都为 OS=16”并不能推导出“OS=16 不是原因”，也不能推导出“OS=16 已解释全部差距”。两者虽然具有相同的输出步长，仍可能在以下方面不同：

- DINOv3 ConvNeXt-T 的中间表征、通道数和有效语义容量更强；
- T1 的 R-ASPP head 是在教师特征上训练得到的，学生 head 只处理 MobileNetV2 特征；
- 教师 feature 的空间定位、上下文和类别分离能力可能使同样的 OS=16 网格更可解码；
- K4/R5 还改变了初始化和监督信号，不能把它们与 T1 的差值归因给 decoder。

因此，若教师也在相同 OS=16 下达到较高小目标 mIoU，最直接的结论是：**OS=16 可能造成共同的空间信息上限，但学生与教师的剩余差距不能由上采样单独解释；学生特征在相同网格上的可解码性必须同时检查。**

#### **猜测二：MobileNetV2 容量不足**

这个猜测同样合理，但当前证据不能证明 MobileNetV2 本身已经达到容量上限。

支持该猜测的事实是：T1 与学生之间仍有较大 mIoU 和 small-object mIoU 差距；S2-0、K4 和 R5 的改进说明监督和蒸馏可以释放性能，但没有接近教师。A6 完整教师空间对学生过难，也说明教师表征与学生可承载空间存在容量或优化难度差异。

不能据此直接下结论的原因是：

- K4 与 R5 的学生损失、初始化和多项蒸馏目标形成了复杂的 Pareto 折中；
- K4 使用 A0 分阶段初始化，R5 目前只有一个 seed，不能把候选差距解释成纯容量效应；
- feature loss 非零可能来自目标空间不完全可拟合，也可能来自损失权重和优化轨迹；
- 现有结果没有同一训练协议下的容量梯度对照。

严格地说，只有在保持数据、head、loss、优化器、训练步数和 seed 全部匹配，仅改变一个预先登记的容量因素后，性能随容量增加而重复改善，且教师差距同步缩小，才能支持容量限制假设。
# 知识蒸馏实验分析与后续实验方向（Cityscapes 语义分割版）

更新日期：2026-08-08

## 0. 更新范围与结论口径

本报告在以下本地材料基础上，将原 ImageNette 10 类图像分类方案迁移到 Cityscapes 语义分割：

- `plan_markdown/实验记录汇总_基于训练日志_260804.md`；
- `plan_markdown/实验记录汇总_基于训练日志_260804.xlsx`；
- `datasets/cityscapes/` 的实际目录和样本；
- `mobilenet论文/Sandler 等 - 2019 - MobileNetV2 Inverted Residuals and Linear Bottlenecks.pdf`；
- `mobilenet论文/Howard 等 - 2019 - Searching for MobileNetV3.pdf`。

旧实验仍能提供机制假设，但不能把 ImageNette 的 Accuracy 数值直接迁移为 Cityscapes 结论：

- 旧结果支持优先研究“固定 PCA 为什么优于当时的可学习 1×1 Conv”；
- 旧结果暴露了随机教师分类头、损失尺度不一致、数据划分反复使用等问题；
- 旧结果不能证明 PCA 在像素级任务中仍然最好，也不能证明 MobileNetV2 一定优于 MobileNetV3；
- 新阶段必须以 19 类 mIoU 为主指标，重新建立教师、学生和无蒸馏基线。

本阶段的主问题调整为：

> DINOv3 ConvNeXt-T 的稠密空间表征，能否通过固定低秩子空间和关系约束，有效迁移到采用论文原始轻量分割头的 MobileNetV2/V3 学生？

这里将 R-ASPP、LR-ASPP 统一称为“分割头”，不再称为“检测头”。

---

## 1. 数据集核验与固定划分

### 1.1 本地数据现状

`datasets/cityscapes/leftImg8bit` 与 `gtFine` 的样本数均符合 Cityscapes 官方划分：

| 官方划分 | 图像数 | 可否本地计算 19 类 mIoU | 本阶段用途 |
|---|---:|---|---|
| train | 2975 | 可以 | 拆为本地 train/dev |
| val | 500 | 可以 | 锁定为本地 test |
| test | 1525 | 不可以 | 仅作最终官网提交 |

本地官方 test 虽然存在 `*_labelIds.png` 和 JSON，但它们不是可用于评分的语义真值。抽查 test mask 只出现 `0/1/3` 等 void ID，同目录多个占位文件哈希相同；因此不能把它们转换成 19 类标签后计算 mIoU。

当前目录没有现成的 `*_labelTrainIds.png`。训练代码应使用 Cityscapes 官方映射把 `labelIds` 转为 `trainIds=0..18`，其余类别统一映射为 `ignore_index=255`。不得把 `color.png` 当作训练标签，也不得把 void 类纳入 mIoU 分母。

### 1.2 推荐的唯一主协议

不建议从官方 train 随机按图片抽一个本地 test。Cityscapes 同一城市中的道路布局、天气和相机序列高度相关，随机图片拆分容易高估泛化能力。

推荐先固定如下协议：

| 本地划分 | 来源 | 城市 | 图像数 | 用途 |
|---|---|---|---:|---|
| `train_local` | 官方 train | 除下列 4 城市外的 14 城市 | 2530 | 训练模型、拟合 Scaler/PCA |
| `dev_local` | 官方 train | darmstadt、jena、krefeld、weimar | 445 | 早停、超参数和模型选择 |
| `test_local` | 官方 val | frankfurt、lindau、munster | 500 | 协议冻结后的本地最终评估 |
| `official_test` | 官方 test | 6 个 test 城市 | 1525 | 可选的一次性官网提交 |

这相当于“从 train 拆开发集，并把官方 val 改作本地 test”，比“从 train 随机拆 test”更合理，原因是：

1. 官方 val 有真实精细标注，能够直接本地评分；
2. train、val 原本按城市隔离，`test_local` 的域独立性更强；
3. `dev_local` 也按完整城市拆分，避免相邻场景泄漏；
4. 官方 test 保持未见，只在确需官方结果时提交。

上述划分已于 2026-08-05 锁定。清单位于 `datasets/cityscapes/train_local.txt`、`dev_local.txt`、`test_local.txt`，每行是以 Tab 分隔的图像/`labelIds` 相对路径；划分规则、城市、数量、日期和各清单 SHA-256 记录在 `datasets/cityscapes/local_splits.lock.json`，标准校验清单为 `local_splits.sha256`。三份 manifest 的组合 SHA-256 为 `033161572be28a6de295e0c5dfb62d83cd4d0a18b6039321347c58ab28b9d3c2`。可运行 `python -B scripts/lock_cityscapes_local_splits.py verify` 重新核验目录、配对、成员关系和文件哈希。PCA、归一化统计、困难样本筛选和类别权重只能使用 `train_local`。

若今后需要官方 test 成绩，可以在本地方案完全冻结后，用同一配置在官方 train+val 上重训一个“官网提交模型”。该模型及官网成绩必须与上述 2530/445/500 本地协议结果分开报告。

---

## 2. 模型定义与公平比较原则

### 2.1 固定三套主模型

| 角色 | Backbone | 分割头 | 用途 |
|---|---|---|---|
| 教师 | DINOv3 ConvNeXt-T | R-ASPP | 教师基线、特征与 logits 来源 |
| 学生 V2 | MobileNetV2 | R-ASPP | A0-A6 的首要机制路线 |
| 学生 V3 | MobileNetV3-Small | LR-ASPP | 最佳方案的跨架构复验 |

MobileNetV3 默认指与旧实验连续的 MobileNetV3-Small。若后续改为 Large，必须另设实验编号，不能与 Small 混记。

V2 使用 R-ASPP、V3 使用 LR-ASPP，是为了遵守两篇论文的轻量语义分割设计。两条学生路线的 head 不同，因此：

- 可以比较完整系统的 mIoU、参数量、MACs、延迟和内存；
- 不能把 V2/V3 差值单独归因于 backbone 或激活函数；
- 激活消融必须在同一个 V2+R-ASPP 系统内部完成；
- A0-A6 先只在 V2 路线上做完整机制消融，再把胜出方案迁移到 V3。

### 2.2 输出步长和特征对齐

主协议统一使用 `output_stride=16`。教师和学生的对齐点不再沿用分类代码中的模块索引 `[0,3,4] -> [2,10,17]`，而按实际张量步长定义：

| 对齐层 | 教师 | 学生 | 目标空间尺度 |
|---|---|---|---|
| L1 | 浅层 stage 输出 | 同步长 block 输出 | OS=4 |
| L2 | 中层 stage 输出 | 同步长 block 输出 | OS=8 |
| L3 | 高层 stage 输出 | 同步长 block 输出 | OS=16 |

实现前必须用一批 `512×1024` crop 做 shape audit，把具体模块名、通道数、张量形状写入配置和结果 JSON。若某层必须插值，只允许固定的双线性插值，并记录 `align_corners=False`；不得为某个实验临时更换层位。

DINOv3 的最深层需要固定为 OS=16 的分割版本，例如取消最后一次下采样并使用膨胀 depthwise convolution。这个改动属于教师架构定义，必须在所有教师和蒸馏实验中保持一致，不能一部分使用原生 OS=32、一部分使用 OS=16。

### 2.3 分割头边界

- R-ASPP：按 MobileNetV2 论文的 reduced ASPP 思路实现，保留 1×1 分支与图像级上下文分支；
- LR-ASPP：按 MobileNetV3 论文实现高层轻量上下文门控和低层 skip；
- 输出通道固定为 19，最终 logits 双线性上采样回原图尺寸；
- 主表只报告单尺度、无水平翻转的推理结果；多尺度/翻转只能作为最终附加结果；
- 蒸馏用 adapter 仅训练时存在，参数量和推理延迟统计前必须移除。

### 2.4 初始化策略

为延续旧研究中“从教师获得表征”的问题，A0-A6 主路线采用 `weights=None` 的学生：

1. 无标签特征蒸馏阶段不使用 mask、教师 logits 或类别信息；
2. 蒸馏后冻结学生 backbone，训练对应的 R-ASPP 分割头；
3. 用 `probe mIoU` 衡量得到的稠密表征；
4. 只对筛选出的少数组再做全模型监督微调，报告 `fine-tune mIoU`。

考虑 Cityscapes 训练图像只有 2530 张，另保留 ImageNette2-320 预训练学生的实用性复验，但不得把 scratch 与 pretrained 结果放在同一均值中，也不得只给蒸馏组使用预训练。这里的 ImageNette2-320 是当前资源约束下可获得的本地预训练方案；相关结果只作为资源受限的实用性基线，不代表更大规模通用数据预训练的效果。

---

## 3. 统一训练与评价协议

### 3.1 数据处理

首轮建议锁定：

- 训练视图：随机缩放 `[0.5, 2.0]`、随机裁剪 `512×1024`、水平翻转；
- 验证/测试：原分辨率 `1024×2048`，单尺度、无翻转；
- teacher/student 必须接收同一张图的同一几何增强；
- 对裁剪后全部为 ignore 的样本重新采样；
- 第一轮不使用会改变类别分布的 class-mix、copy-paste 或伪标签；
- global batch、总 optimizer step、增强、学习率策略在同一学生路线内完全相同。

具体 optimizer、学习率和训练步数先通过基线阶段确定，然后冻结。建议以固定总 step 训练、按 `dev_local mIoU` 选 checkpoint，而不是让每个方法因 early stopping 获得不同训练预算。

### 3.2 损失定义

监督分割损失统一为 19 类像素交叉熵：

\[
L_{seg}=CE(y_s, y; ignore\_index=255)
\]

像素级响应蒸馏使用已训练并冻结的教师 R-ASPP logits：

\[
L_{logit}=T^2\,KL\left(softmax(z_t/T)\;||\;softmax(z_s/T)\right)
\]

教师和学生 logits 必须先对齐到同一空间尺寸，KL 只在非 ignore 像素上取均值。首轮固定 `T=4`、`lambda_logit=0.5`，只有 K2/K3 显示稳定收益后才调参。

多层特征损失按层、通道和像素做均值，避免浅层因元素数更多而支配总损失：

\[
L_{feat}=\frac{1}{3}\sum_{l=1}^{3}\operatorname{mean}\left(\hat f_s^l-\hat f_t^l\right)^2
\]

初始使用 `lambda_feat=1.0`。训练日志同时记录 CE、各层 feature loss、KL 和它们对同一学生层的梯度范数；若辅助项梯度长期超过主 CE 梯度，先统一缩放后再进入正式对比。

### 3.3 评价指标

主指标和必报结果为：

| 类别 | 指标 |
|---|---|
| 分割质量 | 19 类 mIoU、每类 IoU、mAcc、pixel accuracy |
| 边界与小目标 | boundary F1；person/rider/car/truck/bus/train/motorcycle/bicycle 的分组 mIoU |
| 稳定性 | 至少 3 个 seed 的 `mean ± std`；同一 test 上的成对 bootstrap 差值 |
| 效率 | 推理时参数量、MACs、峰值显存、目标设备 batch=1 延迟 |
| 表征诊断 | 各对齐层 CKA、有效秩、均值/方差、PCA 残差率 |

保存 19×19 confusion matrix 和逐图混淆计数，不能只保存一个 mIoU。测试时要对整张图聚合 confusion matrix 后再计算官方口径 mIoU。

---

## 4. 基线准备

在任何 PCA 或关系蒸馏前，必须完成以下基线：

| 编号 | 模型 | 训练方式 | 目的 |
|---|---|---|---|
| T0 | DINOv3+R-ASPP | 冻结 backbone，只训 head | 测量冻结 DINO 稠密表征 |
| T1 | DINOv3+R-ASPP | head warm-up 后解冻最后 stage | 获得可信教师候选 |
| S2-F | 随机 MobileNetV2 冻结+R-ASPP | 只训 head | A 组 probe 下界 |
| S2-0 | MobileNetV2+R-ASPP | scratch、端到端 CE | V2 无蒸馏基线 |
| S3-0 | MobileNetV3-Small+LR-ASPP | scratch、端到端 CE | V3 无蒸馏基线 |
| S2-P/S3-P | 对应学生 | ImageNette2-320 预训练、端到端 CE | 次级实用性基线；与 scratch 分表 |

T0/T1 只在 `dev_local` 选教师，锁定后保存完整权重、配置和哈希。教师未达到稳定且明显高于 S2-0/S3-0 的 mIoU 前，不进入 logits 蒸馏。选定教师不需要在 A0-A6 开始前查看 `test_local`；应在全部协议和候选冻结后，与最终学生候选一起做一次 test 评估。

### 4.1 test_local 的使用规则

`test_local` 是从官方 val 固定出的 500 张最终留出集，只用于最终泛化报告，不用于开发阶段的模型或方案选择。T1 教师、S2-0 scratch 基线和纳入报告的 S2-P（ImageNette2-320 预训练）都应在同一最终评估批次中测试；如果它们各有 3 个 seed，建议对 3 个 seed 全部测试并报告 `mean ± std`，不能按 test 结果挑最高 seed。

因此，当前阶段应继续只使用 `dev_local` 推进 A0-A6、K0-K3 及后续筛选。待数据清单、代码、教师 checkpoint、层位、PCA、损失和候选名单冻结后，再查看 `test_local`。查看后不得再据此调整实验设置。Cityscapes 官方 test 没有可用本地真值，不能计算本地 mIoU；如需官网成绩，只能在本地协议冻结后单独提交一个最终模型。

旧分类实验中的随机教师分类头问题必须彻底消除：K2/K3 的教师 logits 只能来自上述已训练、验证并冻结的 19 类 R-ASPP head。

---

## 5. A0-A6：PCA 与 1×1 Conv 的分割版机制消融

### 5.1 PCA 拟合方式必须改变

分类阶段的 `pca_sample_num=9000` 实际是空间 token 数，浅层可能只覆盖极少图片。Cityscapes 分辨率更高，若直接展平又会被 road、building、sky 等大区域支配。

主协议要求：

- 仅从 `train_local` 拟合；
- 每层跨全部训练图像做 image-balanced reservoir sampling；
- 每张图每层最多 128 个 token，总上限建议 200,000 token/层；
- 抽样不读取类别标签，保持主实验为 label-free representation distillation；
- 用在线均值/方差和 IncrementalPCA 或等价的分批算法，避免一次性保存所有特征；
- 保存抽样 seed、图片清单、每图 token 数、Scaler/PCA 参数及哈希；
- 对每层记录累计解释方差，但不把它等同于语义保留率。

Cityscapes 的稀有类别像素很少，纯方差 PCA 可能舍弃小目标方向。因此在 A0-A6 完成后，可以额外做“按类别均衡采样 PCA”作为有标签扩展，但必须明确标记为 supervised PCA，不能与无标签主线混称。

### 5.2 实验矩阵

A0-A6 首轮只在 MobileNetV2+R-ASPP 上进行，固定 OS=4/8/16 三层、数据视图、训练步数、优化器和 feature MSE：

| 编号 | 教师侧 | 学生侧 | 投影状态 | 回答的问题 |
|---|---|---|---|---|
| A0 | StandardScaler+PCA，T→S 原生通道数 | 无 adapter | 固定 | 迁移旧 PCA 主基线 |
| A1 | 把 A0 的 Scaler+PCA 精确写入 1×1 Conv | 无 adapter | 固定 | 证明 1×1 Conv 可数值复现 PCA |
| A2 | 同 A1，PCA 初始化 | 无 adapter | 可训练、adapter LR=学生 LR 的 0.1 倍 | 固定目标是否是关键正则 |
| A3 | StandardScaler+随机正交 T→S | 无 adapter | 固定 | 主成分是否优于任意稳定低秩子空间 |
| A4 | 不做 StandardScaler，仅 PCA T→S | 无 adapter | 固定 | 尺度校准贡献多大 |
| A5 | StandardScaler+固定 PCA 到 `d_l=C_s^l` | 学生 1×1 Conv 到同一 `d_l` | 只训练学生侧 | 学生保留自身坐标是否更合适 |
| A6 | StandardScaler 后的完整教师空间 | 学生 1×1 Conv，S→T | 可训练 | 公平复测传统完整教师空间对齐 |

A1 在正式训练前必须通过等价性测试：同一输入上 A0 与 A1 每层输出的最大绝对误差不超过 `1e-5`。若失败，不得把训练差异解释为“固定 Conv 与 PCA 不同”。

每个 A 组先进行无标签 feature pretraining，再冻结 backbone 训练同一个 R-ASPP probe。A0、A5、A6 及单种子领先组扩展到 3 个 seed，并增加统一的端到端 fine-tune 结果。

结果解释沿用但改为 mIoU：

- A1≈A0：PCA 优势不来自 1×1 Conv 的表达能力；
- A3≈A0：主要收益来自固定低秩与条件改善，而不是主方向本身；
- A2<A0：可移动教师目标破坏了固定锚定；
- A4<A0：StandardScaler 是重要组成；
- A5>A0：共享瓶颈比强迫学生进入教师坐标更合适；
- A6 仍明显落后：完整教师空间对小学生过难的判断才获得支持。

---

## 6. K0-K3：特征与响应知识的 2×2 实验

A 组回答投影机制，K 组回答实际监督分割中是否需要教师 logits。K0-K3 必须从相同初始化开始、端到端训练、始终使用相同的 hard-label CE：

| 编号 | 硬标签 CE | 最佳 feature KD | 有效教师 logits KD | 作用 |
|---|---|---|---|---|
| K0 | 是 | 否 | 否 | 无蒸馏学生基线，等价于受控重跑 S2-0 |
| K1 | 是 | 是 | 否 | 纯中间特征蒸馏增益 |
| K2 | 是 | 否 | 是 | 纯像素响应蒸馏增益 |
| K3 | 是 | 是 | 是 | 特征+响应蒸馏 |

feature KD 使用 A 组胜出的唯一方案；若 A 组尚未完成，暂以 A0 为默认。首轮固定 `T=4`、`lambda_logit=0.5`、`lambda_feat=1.0`，辅助损失在前 5% 总 step 线性 warm-up。

必须比较：

- `K1-K0`：不使用教师 logits 时，中间特征的收益；
- `K2-K0`：像素级响应知识的收益；
- `K3-K1`：已有特征知识后，logits 是否仍有增量；
- `K3-K2`：feature KD 是否提供 logits 之外的信息。

只有 K2/K3 在至少 3 个 seed 上稳定受益，才搜索温度与 KL 权重。不得先对 K3 大网格调参，再拿未经同等调参的 K0/K1 比较。

---

## 7. 关系知识、分布对齐与激活实验

### 7.1 关系知识优先于 GAN

Cityscapes 的目标是保留空间结构和小目标，关系项应建立在最佳 feature KD 上，而不是替代点对点约束。

| 编号 | 主损失 | 新增项 | 要求 |
|---|---|---|---|
| R0 | CE+最佳 feature KD | 无 | K1 的受控复现 |
| R1 | 同 R0 | batch 内图像关系 | 对 masked GAP 后的 `B×B` 余弦矩阵做 MSE |
| R2 | 同 R0 | 图内空间关系 | 将特征池化到 8×16 后比较 token 余弦矩阵 |
| R3 | 同 R0 | R1+R2 | 使用各自筛出的较小权重 |
| R4 | CE | relation-only | 仅作欠约束诊断，不作为主候选 |

关系项先用 `lambda_rel=0.03`，并监控梯度范数，使其对目标学生层的梯度约为主 feature loss 的 5%–20%。旧相关矩阵实验因在高分辨率上对 Frobenius 归一化矩阵再次取全元素 mean，梯度过小；新实现不能重复该缩放问题。

### 7.2 分布对齐顺序

按以下顺序开展：

1. CORAL；
2. MMD 或 Sliced Wasserstein；
3. 最后才是低权重对抗蒸馏。

GAN 不能替代配对 feature loss，因为它只能约束边缘分布相似，不能保证同一图像、同一位置的语义对应。若做对抗项，应在共同瓶颈的池化特征上使用小判别器，保留 MSE 锚点，并至少 3 个 seed 检查是否增大方差。

KPCA 同样暂缓。旧 poly-KPCA 实现存在特征值缩放和“累计比例必为 1”的指标问题；只有 A0-A6 完成且线性瓶颈的结论明确后，才值得修正实现并重新测试。

### 7.3 激活消融

激活函数只在 MobileNetV2+R-ASPP 内做单变量实验：

| 编号 | 学生激活 | 其余条件 |
|---|---|---|
| H0 | ReLU6 | 最佳 V2 蒸馏协议 |
| H1 | Hardswish | 同 H0 |
| H2 | SiLU/Swish | 同 H0 |
| H3 | h-GELU | 同 H0 |

只替换 inverted residual 中原有非线性，linear bottleneck 末端继续不加激活。除 mIoU 外必须测真实设备 FP32/INT8 延迟；h-GELU 数学上更接近 GELU，不代表部署后端一定能融合。

---

## 8. MobileNetV3 复验规则

完整跑 A0-A6 之后，把以下最小集合迁移到 MobileNetV3-Small+LR-ASPP：

1. S3-0：CE-only；
2. M3-A0：固定 PCA 基线；
3. M3-Abest：V2 上最佳投影机制；
4. M3-K1：CE+最佳 feature KD；
5. M3-K2：CE+logits KD；
6. M3-K3：CE+feature+logits KD。

V3 的 PCA 目标维度必须按 V3 各层通道重新拟合，不能复用 V2 的 PCA 矩阵。教师、数据清单、增强、评价代码和 test 协议不变。V2 与 V3 的最终比较必须同时给出 head、初始化、参数量、MACs 和延迟，不能只排 mIoU。

---

## 9. 推荐执行顺序与停止条件

### 阶段 P0：协议和实现验收

1. 生成 2530/445/500 清单、哈希和类别统计；
2. 验证 labelIds→trainIds、ignore=255 和官方 mIoU 口径；
3. 核对三个 backbone 的 OS=4/8/16 张量；
4. 核对 R-ASPP/LR-ASPP 输出尺寸和参数量；
5. 建立统一配置、结果 JSON、逐图 confusion matrix 和计时脚本。

### 阶段 P1：基线

完成 T0、T1、S2-F、S2-0、S3-0。教师不稳定或不强于学生时，先修复教师，禁止用弱教师继续堆 KD 损失。

### 阶段 P2：投影机制

A0-A6 单 seed 筛选；A0、A5、A6 和领先组做 3 seed。A1 主要是数值等价性验收，不需要重复消耗算力。

### 阶段 P3：知识来源

使用已锁定教师和最佳 feature adapter 完成 K0-K3 的 3 seed 对比。只在 K2/K3 明确有效后搜索 `T` 或 KL 权重。

### 阶段 P4：结构知识与跨学生复验

先 R1，再 R2/R3；随后把 A/K 胜出方案迁移到 MobileNetV3-Small+LR-ASPP。

### 阶段 P5：低优先级扩展

顺序为 CORAL→MMD/SWD→GAN，以及激活/部署实验。KPCA、自定义 ConvNeXt-like 学生和复杂几何损失不进入第一轮主线。

筛选规则：

- 开发阶段每个新想法先用同一个固定 seed；
- 只有 dev mIoU 提升、训练曲线稳定或能回答关键机制问题时才扩到 3 seed；
- test_local 不用于挑 adapter、loss、层位、温度、激活、教师 checkpoint 或学生 seed；
- 最终只对预注册的少数候选查看 test_local；教师、S2-0 以及需要报告的 S2-P 应与最终候选在同一批次完成评估；
- 小于 seed 波动的差值描述为“性能相近”，不按单次最高值宣布胜出。

---

## 10. 每次运行必须保存的内容

- 实验编号、完整配置、代码版本和命令；
- 数据清单文件及 SHA-256；
- seed、初始化来源、global batch、总 step、学习率策略；
- 教师/学生具体层名、OS、通道和张量尺寸；
- Scaler/PCA/adapter 参数、抽样清单和哈希；
- 每个损失分量及关键梯度范数曲线；
- dev 最佳 epoch/step、19 类 IoU、逐图 confusion matrix；
- 模型权重和 SHA-256；
- 推理时移除 adapter 后的参数量、MACs、峰值内存和延迟；
- 是否使用 multi-scale、flip、预训练和官方 test 提交。

同一实验编号的定义不得在中途变化。若改变 crop、输出步长、层映射、head、初始化或数据清单，必须新建编号。

---

## 11. 当前最推荐的主线

近期最值得投入的方案是：

> **DINOv3+R-ASPP 可信教师 + 固定教师低秩子空间 + 学生侧共享投影 + 像素级监督分割，并用小权重空间关系补充边界结构。**

具体顺序为：

1. 先用 MobileNetV2+R-ASPP 解释 PCA、固定 1×1 Conv 和可学习 adapter 的差异；
2. 用 K0-K3 分离 feature KD 与有效 pixel-logit KD 的贡献；
3. 在最佳 feature KD 上加入低成本空间关系；
4. 把胜出方案迁移到 MobileNetV3-Small+LR-ASPP；
5. 最后才研究激活、非线性分布对齐和 GAN。

与旧 ImageNette 阶段相比，新的科学价值不再只是“分类准确率是否提高”，而是同时观察：

- 教师的多尺度空间结构是否被学生保留；
- PCA 是否伤害稀有类别和物体边界；
- feature KD 与 pixel-logit KD 是否互补；
- 相同蒸馏机制能否跨 R-ASPP/LR-ASPP 和 V2/V3 稳定成立；
- mIoU 收益是否值得实际移动端的延迟与内存成本。

具体运行要求和实验编号见 `plan_markdown/Cityscapes知识蒸馏实验详单.md`。
